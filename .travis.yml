---
language: ruby
rvm:
  - 2.6.3
services:
  - postgresql
before_script:
  - psql -c 'create database roda_test;' -U postgres
  - npm install
script:
  - bundle exec rake db:setup
  - bundle exec rake
