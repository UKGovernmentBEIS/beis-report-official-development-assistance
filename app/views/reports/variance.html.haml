=content_for :page_title_prefix, t("page_title.report.variance",report_description: @report_presenter.description, report_financial_quarter: @report_presenter.financial_quarter_and_year)

%main.govuk-main-wrapper#main-content{ role: "main" }

  = render "reports/meta"

  .govuk-grid-row
    .govuk-grid-column-full
      .govuk-tabs
        %h2.govuk-tabs__title
          Contents

        = render partial: "reports/tab_list", locals: { active_tab: "variance" }

        .govuk-tabs__panel
          %h2.govuk-heading-l
            = t("tabs.report.variance.heading")

          %p.govuk-body
            = t("tabs.report.variance.body")

          = render partial: "shared/reports/table_variance", locals: { activities: @activities, readonly: false }
