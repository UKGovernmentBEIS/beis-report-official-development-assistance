%ul.govuk-tabs__list{role: "tablist", aria: { label: "Report subnavigation" } }
  = render partial: "reports/tab", locals: { name: "summary", path: report_path(@report), active_tab: active_tab }
  = render partial: "reports/tab", locals: { name: "activities", path: report_activities_path(@report), active_tab: active_tab }
  = render partial: "reports/tab", locals: { name: "forecasts", path: report_forecasts_path(@report), active_tab: active_tab }
  = render partial: "reports/tab", locals: { name: "actuals", path: report_actuals_path(@report), active_tab: active_tab }
  = render partial: "reports/tab", locals: { name: "variance", path: report_variance_path(@report), active_tab: active_tab }
  = render partial: "reports/tab", locals: { name: "budgets", path: report_budgets_path(@report), active_tab: active_tab }
  = render partial: "reports/tab", locals: { name: "comments", path: report_comments_path(@report), active_tab: active_tab }
