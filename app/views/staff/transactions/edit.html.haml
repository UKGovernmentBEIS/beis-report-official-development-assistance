=content_for :page_title_prefix, t("page_title.transaction.edit")

%main.govuk-main-wrapper#main-content{ role: "main" }
  .govuk-grid-row
    .govuk-grid-column-two-thirds
      %h1.govuk-heading-xl
        = t("page_title.transaction.edit")

      = form_with model: @transaction, url: activity_transaction_path(@activity) do |f|
        = render partial: "form", locals: { f: f, organisations: @organisations }
