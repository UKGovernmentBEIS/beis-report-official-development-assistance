- if grouped_reports.empty?
  = render partial: "staff/shared/reports/table_empty"
- else
  %table.govuk-table
    = render partial: "staff/shared/reports/table_header", locals: { type: type }

    %tbody.govuk-table__body
      - grouped_reports.each do |organisation, reports|
        - reports.each_with_index do |report, index|
          = render partial: "staff/shared/reports/table_row", locals: { report: report, reports: reports, index: index, type: type }
