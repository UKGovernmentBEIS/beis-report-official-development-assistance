= render layout: "wrapper" do |f|
  = f.govuk_collection_check_boxes :intended_beneficiaries,
  intended_beneficiaries_checkbox_options,
  :code,
  :name,
  legend: { tag: 'h1', size: 'xl', text: t("form.legend.activity.intended_beneficiaries") }
