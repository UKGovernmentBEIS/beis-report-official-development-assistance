= f.govuk_error_summary
= f.govuk_collection_radio_buttons :budget_type,
  budget_type_options,
  :code,
  :name,
  :description,
  legend: { tag: :h2 }

= f.govuk_collection_radio_buttons :providing_organisation_id,
  list_of_delivery_partners,
  :id,
  :name,
  legend: { tag: :h2 }

= f.govuk_collection_select :financial_year,
  list_of_financial_years,
  :id,
  :name,
  options: { selected: f.object.financial_year, prompt: t("form.prompt.budget.financial_year") },
  label: { size: "m", tag: "h2" }

= f.govuk_text_field :value, class: "govuk-input govuk-input--width-10"

= f.govuk_submit t("default.button.submit")
