= render layout: "wrapper" do |f|
  = f.hidden_field :has_linked_activity, value: nil
  = f.govuk_collection_radio_buttons :has_linked_activity,
    has_linked_activity_radio_options,
    :value,
    :label,
    legend: { tag: 'h1', size: 'xl', text: @page_title }
