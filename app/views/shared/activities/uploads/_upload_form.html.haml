.govuk-grid-row
  .govuk-grid-column-full
    = form_with model: model, url: send(path_helper, model) do |f|

      = f.govuk_file_field :activity_csv,
        label: { text: t("form.label.activity.csv_file_recover_from_error") },
        hint: { text: t("form.hint.activity.csv_file_recover_from_error_html", link: send(path_helper, model, format: :csv)) }

      = f.govuk_submit t("action.activity.upload.button")
